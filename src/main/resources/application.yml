server:
  servlet:
    context-path: /cadastro-pedidos-api/v1
  port: 9000

spring:
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        use_sql_comments: true
        format_sql: true
        type: trace
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5434/cadastro_pedidos?createDatabaseIfNotExist=true
    username: postgres
    password: ep25dbbr
